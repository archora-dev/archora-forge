openapi: 3.0.3
info: { title: Forge Demo API, version: 1.1.0 }
paths:
  /members:
    get:
      operationId: listMembers
      tags: [Members]
      responses:
        "200":
          description: Members
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required: [id]
                  properties:
                    id: { type: string }
